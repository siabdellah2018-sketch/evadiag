# التقويم التشخيصي — Test diagnostique

تطبيق كامل (يعمل فعليًا، وليس نموذج معاينة) لإدارة الاختبارات التشخيصية:
حسابات ثابتة للتلاميذ، رفع أسئلة PDF، مؤقّت حقيقي، منع اجتياز الاختبار مرتين،
ونتائج يديرها الأستاذ. مجاني بالكامل عبر Vercel + Supabase.

## الخطوات (بالترتيب، بدون أي برمجة مطلوبة منك)

### 1) إنشاء مشروع Supabase (قاعدة البيانات) — مجاني
1. اذهب إلى https://supabase.com وأنشئ حسابًا (بدون بطاقة بنكية)
2. أنشئ مشروعًا جديدًا (New project) — اختر أي اسم وكلمة سر لقاعدة البيانات (احفظها جانبًا)
3. من القائمة الجانبية: SQL Editor > New query
4. افتح ملف `supabase/schema.sql` الموجود في هذا المجلد، انسخ كل محتواه، الصقه، ثم اضغط Run
5. من Settings > API، انسخ ثلاث قيم ستحتاجها لاحقًا:
   - Project URL → ستكون NEXT_PUBLIC_SUPABASE_URL
   - anon public key → ستكون NEXT_PUBLIC_SUPABASE_ANON_KEY
   - service_role key (اضغط Reveal) → ستكون SUPABASE_SERVICE_ROLE_KEY (لا تشاركها مع أحد أبدًا)

### 2) رفع المشروع على GitHub
1. أنشئ حسابًا على https://github.com إن لم يكن لديك
2. أنشئ مستودعًا (repository) جديدًا فارغًا
3. ارفع كل ملفات هذا المجلد إليه (يمكنك استعمال زر "Upload files" في واجهة GitHub مباشرة، بدون أي أوامر)

### 3) الاستضافة على Vercel — مجاني
1. اذهب إلى https://vercel.com وسجّل الدخول بحساب GitHub
2. اضغط "Add New" > "Project" واختر المستودع الذي رفعته
3. قبل الضغط على Deploy، افتح قسم "Environment Variables" وأضف:
   - `NEXT_PUBLIC_SUPABASE_URL` = (القيمة من الخطوة 1)
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = (القيمة من الخطوة 1)
   - `SUPABASE_SERVICE_ROLE_KEY` = (القيمة من الخطوة 1)
   - `TEACHER_ACCESS_CODE` = اختر أنت كلمة سر قوية تستعملها للدخول كأستاذ
4. اضغط Deploy وانتظر دقيقة أو دقيقتين
5. ستحصل على رابط تطبيقك (مثال: `https://your-app.vercel.app`) — هذا هو رابط تطبيقك الدائم والمجاني

## كيف تستعمل التطبيق بعد النشر

1. افتح الرابط > اختر "أستاذ(ة)" > أدخل TEACHER_ACCESS_CODE الذي اخترته
2. في "حسابات التلاميذ": الصق لائحة تلاميذك بالصيغة `الاسم الكامل ; رمز Massar ; كلمة السر` (سطر لكل تلميذ)
3. في "اختبار جديد": ارفع ملف PDF، حدد المدة وعدد الأسئلة والأجوبة الصحيحة، احصل على كود الاختبار
4. شارك كود الاختبار مع تلاميذك — كل تلميذ يدخل بـ: اسمه الكامل + رمز Massar + كلمة سره + كود الاختبار
5. في "النتائج": أدخل كود الاختبار لمتابعة نتائج التلاميذ، وإظهار الأجوبة الصحيحة إن أردت، ومنح محاولة إضافية لأي تلميذ

## حدود يجب معرفتها
- قاعدة بيانات Supabase المجانية تتوقف تلقائيًا (pause) بعد 7 أيام بدون أي نشاط — إعادة التفعيل بضغطة زر واحدة من لوحة Supabase، بدون فقدان أي بيانات
- التحقق من الهوية هنا مبسّط (مناسب لفصل دراسي) وليس بنفس مستوى حماية تطبيقات بنكية — كافٍ تمامًا لهذا الاستعمال
- ملفات PDF تُخزَّن كعامة (public) عبر رابط غير قابل للتخمين — إذا أردت لاحقًا حمايتها بشكل أكثر صرامة يمكن تطويرها

---

## التحديث v2 — ميزات جديدة

هذا التحديث **لا يمسح أي بيانات موجودة**. اتبع الخطوتين التاليتين فقط:

### 1) تحديث قاعدة البيانات (مرة واحدة)
في Supabase: SQL Editor > New query > الصق محتوى ملف `supabase/migration_v2.sql` > Run

### 2) رفع الملفات الجديدة/المحدثة إلى GitHub
ارفع كل محتوى هذا المجلد فوق الملفات القديمة في مستودعك (استبدال). Vercel سينشر النسخة الجديدة تلقائيًا خلال دقيقتين.

## الميزات الجديدة

- **تصدير Excel/PDF**: من "النتائج"، أزرار لتصدير الجدول كاملاً (Excel) أو أوراق إجابة كل تلميذ (PDF فردي أو جماعي حسب القسم)
- **تحليل حسب السؤال**: نسبة الخطأ في كل سؤال، لتحديد الدروس التي تحتاج مراجعة
- **رسم توزيع النقط**: ضعيف / متوسط / جيد
- **المستوى والقسم**: أضف عمودين جديدين عند إنشاء التلاميذ (بصيغة CSV بخمس حقول: الاسم ; رمز Massar ; كلمة السر ; المستوى ; القسم)، ويمكن التصفية حسب القسم في النتائج
- **سجل الاختبارات**: تبويب جديد "السجل" يعرض كل الاختبارات وتاريخها وعدد المشاركات، مع زر مباشر لعرض النتائج
- **استيراد/تصدير Excel للتلاميذ**: تبويب "التلاميذ" يقبل الآن رفع ملف Excel (أعمدة: الاسم الكامل، رمز Massar، كلمة السر، المستوى، القسم) وتصدير اللائحة الحالية
- **منع تحميل PDF**: عارض الأسئلة الآن بدون شريط أدوات أو زر تحميل ظاهر (ملاحظة: هذا رادع وليس حماية مطلقة، لأن رابط الملف يبقى عامًا تقنيًا)
- **حفظ تلقائي**: إجابات التلميذ تُحفظ فور اختيارها في متصفحه، وتُسترجع تلقائيًا إذا أُغلقت الصفحة أو انقطع الاتصال قبل التسليم
- **تنبيه 5 دقائق**: لون المؤقت يتحول إلى برتقالي عند 5 دقائق متبقية، وأحمر نابض عند آخر دقيقة
- **حسابات أساتذة إضافية**: من تبويب "الأساتذة" (يظهر فقط لك كحساب رئيسي)، يمكنك إضافة اسم + كلمة سر لأي زميل، وسيدخل هو بحسابه الخاص بدل الكود الرئيسي

---

## التحديث v3 — المستويات والأقسام + الحذف

**لا يمسح أي بيانات موجودة.** خطوتان فقط:

### 1) قاعدة البيانات
في Supabase SQL Editor، شغّل محتوى `supabase/migration_v3.sql` (يُنشئ المستويات الخمسة الافتراضية تلقائيًا، ويضيف الربط بين التلاميذ/الاختبارات والمستويات والأقسام)

### 2) رفع الملفات
ارفع كل الملفات فوق القديمة في GitHub كالمعتاد.

## الميزات الجديدة v3

- **المستويات جاهزة مسبقًا**: Tronc Commun Scientifique، 1BAC-SE، 1BAC-SM، 2BAC-PC/SVT، 2BAC-SM — ويمكنك من تبويب "المستويات والأقسام" إضافة/تعديل/حذف أي مستوى
- **الأقسام داخل كل مستوى**: أنشئها من نفس التبويب (تعديل/حذف أيضًا متاحان)
- **إنشاء التلاميذ**: عند كتابة اسم مستوى/قسم غير موجود في اللائحة النصية أو ملف Excel، يُنشأ تلقائيًا — لكن التعديل والحذف الدقيقين يبقيان من تبويب "المستويات والأقسام"
- **إنشاء الاختبار**: أصبح يتطلب اختيار مستوى من القائمة المنسدلة (بدل الكتابة الحرة)
- **حذف التلاميذ**: من تبويب "التلاميذ"، زر حذف بجانب كل تلميذ في اللائحة
- **حذف الأساتذة**: من تبويب "الأساتذة" (الحساب الرئيسي فقط)، زر حذف بجانب كل حساب
